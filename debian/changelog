stawk (1.2) unreleased; urgency=medium

  * UNRELEASED

 -- Dustin Kirkland <kirkland@ubuntu.com>  Thu, 27 Jul 2017 12:55:15 -0500

stawk (1.1-0ubuntu1) artful; urgency=medium

  * === added symlink bin/avg, === added symlink bin/histogram, ===
    added symlink bin/max, === added symlink bin/mean, === added symlink
    bin/median, === added symlink bin/min, === added symlink bin/mode,
    === added symlink bin/stddev, === added symlink bin/stdev, === added
    symlink bin/summate, bin/stats => bin/stawk, debian/control,
    debian/copyright, === removed symlink bin/avg, === removed symlink
    bin/histogram, === removed symlink bin/max, === removed symlink
    bin/mean, === removed symlink bin/median, === removed symlink
    bin/min, === removed symlink bin/mode, === removed symlink
    bin/stddev, === removed symlink bin/stdev, === removed symlink
    bin/summate, share/man/man1/stats.1 => share/man/man1/stawk.1, ===
    target is ustawk, === target was ustats, snap/*:
    - rename the project because "statistics" was deemed too generic

 -- Dustin Kirkland <kirkland@ubuntu.com>  Thu, 27 Jul 2017 12:55:13 -0500

statistics (1.0-0ubuntu1) artful; urgency=medium

  * Initial release

 -- Dustin Kirkland <kirkland@ubuntu.com>  Wed, 26 Jul 2017 17:48:26 -0500
